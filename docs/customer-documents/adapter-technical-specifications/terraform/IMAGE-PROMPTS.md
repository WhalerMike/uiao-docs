# Image Prompts — Terraform / OpenTofu Evidence Adapter (ATS)

> Scaffold generated by `sync_canon.py`. Prompts follow the
> `## IMAGE-NN` / `## DIAGRAM-NN` heading convention consumed by
> `tools/aggregate_prompts.py`. When images are produced they land
> in `./images/`.

## IMAGE-01

A 16:9 muted-blue schematic showing the Terraform / OpenTofu adapter
as an Integration-class node in the UIAO adapter plane. At center, a
Terraform-orange diamond icon labeled "TerraformAdapter" with three
inbound arrows from the left representing data sources (labeled
"tfstate", "plan JSON", "HCL config") and one outbound arrow to the
right feeding into a canonical-blue "ClaimSet" cylinder. A thin
certificate-chain line connects the adapter node back to a small SSOT
badge at upper-right. The GCC-Moderate governance perimeter is drawn
as a dashed rounded rectangle enclosing the entire composition. No
text baked into the image body. Publication-grade, flat design, no
gradients, accessible contrast.

## IMAGE-05

A 16:9 muted-blue icon representing the **Integration Adapter Class**
(UIAO_003 s4.7). A wrench-and-gear motif superimposed on a target
reticle, signifying change-making actions against a target
environment. An outbound arrow from the wrench exits toward a
stylized "target system" rectangle at right, while a return arrow
carries a certificate-chain badge labeled "change record" back toward
a small SSOT badge at upper-left. The color palette uses the standard
UIAO muted-blue tones with a subtle accent on the wrench to
distinguish it from the Enforcement class icon (IMAGE-04, which uses
a seal motif). No text baked into the image. Publication-grade, flat
design, accessible contrast. This icon represents all modernization
adapters classified as `mission-class: integration` in the canon
registry (entra-id, m365, service-now, palo-alto, scuba, terraform).

## DIAGRAM-01

A 16:9 muted-blue schematic showing the UIAO Adapter Segmentation
model with **five** named adapter classes arranged symmetrically
around a singular, certificate-anchored SSOT core at center:

1. **Identity** (top) — shield icon, inbound certificate arrows
2. **Telemetry** (upper-right) — observation-lens icon, outbound
   timestamped records
3. **Policy** (lower-right) — rulebook icon, inbound observations
   and outbound evaluated outcomes
4. **Enforcement** (lower-left) — machine-seal icon, producing
   signed assurance artifacts toward an external "AO / Assessor" node
5. **Integration** (upper-left) — wrench-on-target icon, producing
   change-making actions toward a "Target Environment" node

Each adapter node has a bidirectional arrow to the SSOT core: outbound
for provenance consumption, inbound for certificate-anchored provenance
records. A dashed GCC-Moderate governance perimeter ring encloses the
entire composition. The Amazon Connect Contact Center exception is
noted as a small "Commercial Cloud" annotation outside the ring with
a single dashed arrow crossing the boundary.

No text baked into the image body. Publication-grade, flat design,
accessible contrast.

## DIAGRAM-02

A 16:9 muted-blue schematic showing the SSOT boundary and certificate
chain architecture. The composition has three horizontal layers:

**Top layer (SSOT):** A singular certificate-anchored SSOT badge with
five outbound certificate chain lines (C1 through C5), one to each
adapter class below.

**Middle layer (Adapter Classes):** Five adapter-class nodes arranged
left to right: Identity, Telemetry, Policy, Enforcement, Integration.
Each node shows its icon (shield, lens, rulebook, seal, wrench) and
is connected to its certificate chain line from the SSOT above.

**Bottom layer (Governance OS Substrate):** A broad horizontal bar
labeled "Governance OS" spanning the full width, with upward support
arrows to each adapter node.

Directional flow arrows show truth flowing downward from SSOT through
the adapters, and provenance records flowing upward from adapters back
to SSOT. The GCC-Moderate boundary is a dashed outline enclosing all
three layers.

No text baked into the image body. Publication-grade, flat design,
accessible contrast.

## DIAGRAM-03

A 16:9 muted-blue schematic showing the unidirectional Cross-Adapter
Truth Flow (UIAO_003 s4.6). The composition flows left to right:

**SSOT** (left) — singular core emitting truth arrows rightward.

**Adapter chain** (center) — five adapter nodes in sequence:
Identity -> Telemetry -> Policy -> Enforcement, with Integration
branching off from Policy as a parallel path. Each arrow is labeled
with the type of record flowing:
- Identity -> Telemetry: "object identifiers"
- Telemetry -> Policy: "timestamped observations"
- Policy -> Enforcement: "policy outcome records"
- Policy -> Integration: "authorized change intent"
- Integration -> (target environment): "create/update/delete actions"

**Return path** (bottom) — dashed arrows from each adapter flowing
back leftward to SSOT, labeled "certificate-anchored provenance
records only."

**Annotations:**
- A "drift signal" callout at the Policy/Enforcement interface
- A "change intent" callout at the Policy/Integration branch point
- A note: "No adapter class rewrites the output of another"

No text baked into the image body. Publication-grade, flat design,
accessible contrast.
